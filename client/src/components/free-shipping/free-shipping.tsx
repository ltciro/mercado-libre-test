import classes from "./free-shipping.module.css";

export default function FreeShipping() {
  return (
    <span className={`material-symbols-outlined ${classes.freeShipping}`}>
      local_shipping
    </span>
  );
}
